# 🔄 Como Atualizar o Site Sempre

## ✅ Sim, precisa enviar via CMD, mas é MUITO SIMPLES!

---

## 🚀 Forma Mais Fácil: Usar o arquivo `atualizar.bat`

### Passo a Passo:

1. **Faça suas alterações no código:**
   - Edite `index.html`, `script.js`, `styles.css`, etc.
   - Teste localmente primeiro (use `iniciar.bat`)

2. **Quando terminar, clique 2x no arquivo:**
   ```
   atualizar.bat
   ```

3. **Pronto!** O arquivo faz tudo automaticamente:
   - Adiciona os arquivos
   - Faz commit
   - Envia para o GitHub
   - O Vercel atualiza automaticamente em 1-2 minutos

---

## 📝 Comandos Manuais (se preferir):

Se quiser fazer manualmente no CMD:

```cmd
cd C:\Users\AiNote\Desktop\site
git add .
git commit -m "Minha atualização"
git push
```

---

## ⏱️ Tempo Total:

- **Usando atualizar.bat:** ~10 segundos (só clicar 2x)
- **Comandos manuais:** ~30 segundos
- **Vercel atualiza:** 1-2 minutos (automático)

---

## 🎯 Resumo:

**SIM, precisa enviar via CMD, MAS:**
- ✅ É muito rápido (só 3 comandos)
- ✅ Pode usar o arquivo `.bat` (só clicar 2x)
- ✅ O Vercel atualiza automaticamente
- ✅ Não precisa fazer nada no Vercel

---

## 💡 Dica:

Sempre que fizer alterações:
1. Teste localmente (use `iniciar.bat`)
2. Quando estiver funcionando, clique 2x no `atualizar.bat`
3. Aguarde 1-2 minutos
4. Teste no site do Vercel

**É isso! Simples assim! 🚀**
